rules:
  unpinned-uses:
    config:
      policies:
        actions/*: ref-pin
        dtolnay/rust-toolchain: ref-pin
        Swatinem/rust-cache: ref-pin
        github/codeql-action/upload-sarif: ref-pin
